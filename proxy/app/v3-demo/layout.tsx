import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "gcp-x402 · Strategy and GCP monitor",
  description: "Paper-strategy telemetry, cloud-resource inventory, estimated cost, lease status, and controls.",
};

export default function V3DemoLayout({ children }: { children: React.ReactNode }) { return children; }
