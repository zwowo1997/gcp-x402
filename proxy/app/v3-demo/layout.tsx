import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "gcp-x402 v3 · Cloud infrastructure checkout rehearsal",
  description: "Private-beta simulation of a Coinbase-style checkout for temporary GCP infrastructure.",
};

export default function V3DemoLayout({ children }: { children: React.ReactNode }) { return children; }
